import express from 'express';

const router = express.Router();

router.post('/date/options',);
router.post('/date/vote',);
router.get('/date/options/:trip_id',);
router.get('GET /date/summary/:trip_id',);